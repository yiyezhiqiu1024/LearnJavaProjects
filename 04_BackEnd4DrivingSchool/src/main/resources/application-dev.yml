spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/driving_school?serverTimezone=GMT%2B8

server:
  port: 8080

logging:
  level:
    com.sl.mapper: debug

ds:
  cfg:
    cors-origins:
      - http://localhost:63342